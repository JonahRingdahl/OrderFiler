name: .NET Build & Test

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-and-test:
    runs-on: windows-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: "10.x"

    - name: Restore dependencies
      run: dotnet restore OrderManager.slnx

    - name: Install dotnet-ef
      run: dotnet tool install dotnet-ef

    # - name: Add dotnet tools to PATH
    #   shell: pwsh
    #   run: echo "$env:USERPROFILE\.dotnet\tools" >> $env:GITHUB_PATH

    # - name: Apply EF Core migrations
    #   working-directory: OrderManagerApp\
    #   run: dotnet ef database update

    # - name: Create database directory
    #   run: mkdir "$env:APPDATA\OrderManager"
    #   shell: pwsh
        
    # - name: Apply EF Core migrations
    #   working-directory: OrderManagerApp\
    #   env:
    #     ORDERDB_PATH: ${{ runner.temp }}/orders.db
    #   run: dotnet ef database update

    - name: Build solution
      run: dotnet build OrderManager.slnx --configuration Release --no-restore

    - name: Run tests
      run: dotnet test OrderManager.slnx --configuration Release --no-build --verbosity normal

    - name: Publish Artifiact
      run: dotnet publish -c Release -r win-x64 -p:SelfContained=true -p:IncludeAllContentForSelfExtract=true

    - name: Get version from csproj
      id: get_version
      shell: pwsh
      run: |
        [xml]$csproj = Get-Content OrderManagerApp/OrderManagerApp.csproj
        $version = $csproj.Project.PropertyGroup.Version
        echo "version=$version" >> $env:GITHUB_OUTPUT
    
    - name: Check if release exists
      id: check_release
      shell: pwsh
      run: |
        $exists = gh release view v${{ steps.get_version.outputs.version }} 2>&1
        if ($LASTEXITCODE -eq 0) {
          echo "exists=true" >> $env:GITHUB_OUTPUT
        } else {
          echo "exists=false" >> $env:GITHUB_OUTPUT
        }
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      continue-on-error: true
    
    - name: Create Release
      if: steps.check_release.outputs.exists == 'false'
      uses: softprops/action-gh-release@v1
      with:
        files: ./bin/Release/net10.0-windows/win-x64/publish/**/*
        tag_name: v${{ steps.get_version.outputs.version }}
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
